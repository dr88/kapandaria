<%= form_for @classified do |f| %>
  <% if @classified.errors.any? %>
    <div id="error_explanation" class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@classified.errors.count, "error") %> prohibited this classified from being saved:</h4>

      <ul>
      <% @classified.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :title %><br>
    <%= f.text_field :title, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :location %><br>
    <%= f.text_field :location, class: "form-control" %>
  </div>
  <!-- <%= f.label :location, "Locations" %>
  <div class="form-group">
    <%= f.text_field :location, name: "classified[location]", class: "form-control", id: "form-field-tags", value: @classified.location %>
  </div> -->
  <div class="form-group">
    <%= f.label :body %><br>
    <%= f.text_area :body, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :last_line %><br>
    <%= f.text_field :last_line, class: "form-control" %>
  </div>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary btn-lg" %>
  </div>
  <%= link_to "Back", classifieds_path %>
<% end %>
